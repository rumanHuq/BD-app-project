// @flow

import React from 'react';
import styled from 'styled-components';

const Header = styled.div`
  background: #f0a;
  grid-area: Header;
`;

export default () => <Header>I am the Header</Header>;
